# Week 11 / Day 2 — MyBatis XML 映射

> 建议用时：60 分钟  
> 今日目标：用 XML 管理复杂 SQL 与结果映射。

## 1. 今天学什么
- namespace、`resultMap`、`#{}`、`<select>`。

## 2. 实践
为 User 查询定义 XML；将列名映射到 Java 字段。

# 3. Day 2 作业
实现用户列表查询，禁止 `${}` 拼接用户输入。

## 4. 今日验收标准
- namespace 对应 Mapper 全限定名。
- 绑定参数使用 `#{}`。

下一节：**Week 11 / Day 3 — 动态 SQL。**
